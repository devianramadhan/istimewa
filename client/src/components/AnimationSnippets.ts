
// This is an inline component snippet to be added to GameRoom.tsx
/*
// WIN OVERLAY
{currentPlayer.finishedRank && (
    <div className="absolute inset-0 z-[60] flex flex-col items-center justify-center pointer-events-none">
        <div className="bg-black/80 absolute inset-0 backdrop-blur-sm animate-fade-in" />
        <div className="relative z-10 text-center animate-bounce-in">
            <h1 className="text-6xl md:text-8xl font-black text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 mb-4 drop-shadow-[0_0_15px_rgba(255,215,0,0.8)] px-4">
                Selamat, Anda Menang!
            </h1>
            <p className="text-2xl text-white font-bold animate-pulse">
                Juara #{currentPlayer.finishedRank}
            </p>
            <div className="mt-8 text-4xl animate-spin-slow">🏆</div>
        </div>
    </div>
)}

// LOSE OVERLAY
{gameState.status === 'finished' && !currentPlayer.finishedRank && (
    <div className="absolute inset-0 z-[60] flex flex-col items-center justify-center pointer-events-none">
        <div className="bg-red-900/80 absolute inset-0 backdrop-blur-sm animate-fade-in" />
        <div className="relative z-10 text-center animate-shake">
            <h1 className="text-5xl md:text-7xl font-black text-rose-500 mb-6 drop-shadow-[0_5px_5px_rgba(0,0,0,0.8)] px-4 rotate-[-5deg]">
                Yha Kalah! <br/> Cupu Bet Dah Luuu!! 🤭
            </h1>
            <div className="text-8xl grayscale opacity-80 mt-4">🤡</div>
        </div>
    </div>
)}
*/
